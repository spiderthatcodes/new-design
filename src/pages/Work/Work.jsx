import React from 'react';
import Layout from '../../components/Layout/Layout';

const Work = ({ isMobile }) => {
    return (
        <Layout isMobile={isMobile}>
            <h1>Work</h1>
        </Layout>
    );
};

export default Work;
